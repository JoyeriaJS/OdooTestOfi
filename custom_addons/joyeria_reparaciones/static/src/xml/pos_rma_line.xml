<odoo>
    <templates id="template" xml:space="preserve">

        <t t-name="joyeria_reparaciones.PosRMALine"
           t-inherit="point_of_sale.Orderline"
           t-inherit-mode="extension">

            <xpath expr="//div[hasclass('info-list')]" position="inside">

                <!-- VALIDACIÓN SEGURA -->
                <t t-if="props.line and props.line.numero_rma">

                    <div style="font-size:12px; opacity:0.7; margin-top:4px;">

                        <div>
                            Subtotal:
                            <t t-out="props.line.subtotal_rma || 0"/>
                        </div>

                        <div>
                            Abono:
                            <t t-out="props.line.abono_rma || 0"/>
                        </div>

                        <div>
                            <b>
                                Saldo:
                                <t t-out="props.line.saldo_rma || 0"/>
                            </b>
                        </div>

                    </div>

                </t>

            </xpath>

        </t>

    </templates>
</odoo>